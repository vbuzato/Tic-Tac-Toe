import React from 'react';
import { render } from '@testing-library/react';
import App from './App';

describe('Verifica se as funções do tabuleiro estão OK', () => {
  test('Testa se todos o board inicia vazio', () => {
    // const { getByText } = render(<App />);
    // expect(getByText(/X/i)).toBeUndefined();
    // expect(getByText(/O/i)).not.toBeInTheDocument();
  });
})
